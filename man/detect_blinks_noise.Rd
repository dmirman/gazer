% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_blinks_noise.R
\name{detect_blinks_noise}
\alias{detect_blinks_noise}
\title{Noise-based blink detection (Hershman, Henik & Cohen, 2018)}
\usage{
detect_blinks_noise(
  pupil,
  hz,
  smooth_ms = 10,
  gap_ms = 100,
  missing = c(0, NA)
)
}
\arguments{
\item{pupil}{Numeric vector of pupil samples (one trial, one eye).
Must be in time order at the rate given by \code{hz}.}

\item{hz}{Sampling rate in Hz.}

\item{smooth_ms}{Centered moving-average window applied before
onset / offset refinement. Default 10 ms.}

\item{gap_ms}{Adjacent blinks closer than this (in ms) are merged.
Default 100 ms.}

\item{missing}{Values in \code{pupil} that should be treated as missing
samples. Default \code{c(0, NA)} -- works for Tobii (0) and EyeLink-style
(NA) exports without configuration.}
}
\value{
An integer vector the same length as \code{pupil}: \code{0} outside
detected blinks, \verb{1, 2, ...} inside each blink. When used inside
\code{dplyr::mutate()} under a \code{group_by()}, blink ids restart at 1 in
each group.
}
\description{
Detects blinks in a per-sample pupil time series. Starts from the
tracker's missing-data sentinel and refines each missing-sample run's
onset and offset by walking outward on the smoothed first-difference
signal -- picking up the eyelid descent before the missing samples and
the recovery rise after. Adjacent blinks separated by less than
\code{gap_ms} are merged.
}
\details{
Tracker-agnostic missing handling: by default treats both \code{NA} and \code{0}
as missing (Tobii uses \code{0}; SR Research / EyeLink exports often use
\code{NA}). Override via \code{missing}.

Composes with dplyr exactly like \code{gazeR::extend_blinks()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{df |>
  group_by(subject, trial) |>
  mutate(blink = detect_blinks_noise(pupil, hz = 250))
}\if{html}{\out{</div>}}

If you want pupil set to \code{NA} inside blinks (gazeR-style), chain one
more line:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{... |>
  mutate(pupil = ifelse(blink > 0, NA_real_, pupil))
}\if{html}{\out{</div>}}

Adapted to R with Dr William Paul Boyce (paul.boyce@ntu.edu.sg);
indexing bug in the original port -- matching on index instead of
time -- was diagnosed by Dr Jason Geller.
}
